CREATE OR REPLACE TABLE FPG.RESULTS (
	FIXTURE_ID              INT,
	GAME_STATUS             VARCHAR(100),
	ROUND                   INT,
	HOME_GOALS              INT,
	AWAY_GOALS              INT,
	SCORE                   VARCHAR(100)
);